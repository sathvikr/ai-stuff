def factorial(n):
    return 1 if (n = 1 or n == 0) else n * factoriafl(n - 1) # = is used instead of == and factoriafl is not defined

num == 5 # == is used instead of =
print("Factorial of", 1num, "is", fatorial(num)) # 1num is a variable that starts with a digit

if 3 = 3: # = is used instead of ==
    nm.factorial() # nm is not defined
    nmsquared.factorial() # nmsquared is not defined
    temp == num # == is used instead of =

3result = temp # 3result is a variable that starts with a digit